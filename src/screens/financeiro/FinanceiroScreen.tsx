import React from 'react';

import { PlaceholderScreen } from '@/components/PlaceholderScreen';

/** Aba "Financeiro" (provisória). */
export function FinanceiroScreen(): React.JSX.Element {
  return (
    <PlaceholderScreen
      title="Financeiro"
      message="Suas mensalidades e comprovantes aparecerão aqui."
    />
  );
}
