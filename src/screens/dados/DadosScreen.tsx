import React from 'react';

import { PlaceholderScreen } from '@/components/PlaceholderScreen';

/** Aba "Dados" (provisória). */
export function DadosScreen(): React.JSX.Element {
  return (
    <PlaceholderScreen
      title="Dados"
      message="Seu perfil e informações pessoais aparecerão aqui."
    />
  );
}
