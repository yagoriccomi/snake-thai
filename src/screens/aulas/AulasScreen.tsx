import React from 'react';

import { PlaceholderScreen } from '@/components/PlaceholderScreen';

/** Aba "Aulas" (provisória). */
export function AulasScreen(): React.JSX.Element {
  return (
    <PlaceholderScreen
      title="Aulas"
      message="Sua agenda de treinos e eventos aparecerá aqui."
    />
  );
}
